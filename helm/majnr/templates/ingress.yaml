apiVersion: traefik.containo.us/v1alpha1
kind: IngressRouteTCP
metadata:
  name: majnr-minecraft
  namespace: majnr
spec:
  entryPoints:
    - minecraft
  routes:
  - match: Host(`majnr.jindrich-jakubicka.cz`)
    priority: 10
    services:
    - name: 
      port: 25565
